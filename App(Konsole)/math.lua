function plus() {
	plus = a + b;
	print("A + B =" + plus);
}

function minus() {
	minus = a - b;
	print("A - B =" + minus);
}

function multiply() {
	multiply = a * b;
	print("A * B =" + multiply);
}

function divide() {
	divide = a / b;
	print("A / B =" + divide);
}

function power() {
	powa = a * a;
	print("power(a) =" + powa);
	
	powb = b * b;
	print("power(b) =" + powb);
	
}